pipeline{
     agent any
     parameters {
        string description: 'enter firstname', name: 'firstName'
        string description: 'enter lastname', name: 'lastName'
     }
     stages{
        stage('check_out_'){
	  steps{
	  	echo "Hi ${params.firstName} ${params.lastName}"
   	  }
        }
     }
}
